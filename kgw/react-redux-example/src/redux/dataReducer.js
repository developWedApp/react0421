
const initialState = {
   todos:[]
}

function dataReducer(state=initialState, action){
    switch(action.type){

        default:
            return state;

    }
}

export {
    dataReducer,
}